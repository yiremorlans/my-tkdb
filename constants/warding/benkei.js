// Benkei has no warding art. This stub keeps ./index.js's import list complete
// (one file per character, same order as constants/dialogue/index.js) so adding
// cards later is only an edit here. A character with an empty map never enters a
// warding pool and never counts toward the pity counter — see ./index.js and
// docs/warding-cards.md.
export default {};
